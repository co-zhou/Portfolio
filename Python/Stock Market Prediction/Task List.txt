1 day - (DONE) Draft project overview and features				
      - (DONE) Create table structure for mysql database

1 day - (DONE) Automate initializing and inserting random data in database
      - (DONE) Display select queries onto web app

1 day - (DONE) Connect web app to gunicorn
      - (DONE) Connect gunicorn to nginx

1 day - (DONE) Add input box to insert user into database

1 day - (DONE) '/' Home page shows list of available train routes
    	    Search by route shows all users on route
    	    Insert new route for user

1 day - (DONE) '/users' Users page shows list of users
	 	 Search by user to see list of routes for that user
	 	 Insert new user
------------------------------------------------------------------------------------------
2 days - (DONE) '/login' Input full name to have permission to see the users information
		  redirect to '/' if inputs correct name 

1 day - (DONE) '/register' add user move from '/users'
		    redirect to '/' logged in to that user

1 day - (DONE) '/' redirect button to '/register'
	'/' button to '/login' if not logged in or logout if already logged in
	redirect button to home
	add route 

1 day - (DONE) '/users' -> '/home' show routes with username
				redirect to '/login' if not logged in
------------------------------------------------------------------------------------------
1 day - Delete account -> delete user routes -> '/logout'
      - token authentication

2 day - LSTM recurrent neural net with keras
1 day - Automatic import SPX updated data and preprocess
1 day - Fine tune parameters
